server:
  port: 8770

spring:
  application:
    name: PROBLEM-SERVICE
  devtools:
    livereload:
      enabled: true

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost
